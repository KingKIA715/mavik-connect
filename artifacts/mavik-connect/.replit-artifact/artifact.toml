kind = "web"
previewPath = "/"
title = "Mavik Connect"
version = "1.0.0"
id = "artifacts/mavik-connect"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20730

[services.development]
run = "pnpm --filter @workspace/mavik-connect run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mavik-connect", "run", "build" ]
publicDir = "artifacts/mavik-connect/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20730"
BASE_PATH = "/"
